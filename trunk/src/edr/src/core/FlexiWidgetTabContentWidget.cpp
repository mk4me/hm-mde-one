#include "CorePCH.h"
#include "FlexiWidgetTabContentWidget.h"

FlexiWidgetTabContentWidget::FlexiWidgetTabContentWidget(QWidget * parent, Qt::WindowFlags f) : QWidget(parent, f)
{
    setupUi(this);
}
